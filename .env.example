## DeepSeek（示例）
DEEPSEEK_API_KEY=sk-your_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

## SQLite 数据库路径（绝对路径）
SQLITE_DB_PATH=C:\path\to\your.db

## 是否输出 SQL Agent debug 日志（0/1）
CHAT2SQL_SHOW_SQL=0

## 本轮结束后是否合并多条 SQL 为单条并做执行校验（0/1）
CHAT2SQL_SQL_MERGE=1

## 合并 SQL 执行失败时，允许 LLM 根据错误信息重试的次数（额外重试次数）
CHAT2SQL_SQL_MERGE_RETRIES=1

